# Greeting implementation: simple.
# Contract: define `.greeting` with a script. jobs/greeting.yml extends it.
.greeting:
  image: alpine:3.20
  script:
    - echo "hello from the $GREETING_MODE greeting module"
